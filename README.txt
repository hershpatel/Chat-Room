This is a simple Chat Room Program that utilizes socket programming (client/server). A client can connect to the server to send messages for all other clients to see.

To compile source code:  make
For server side:  ./server <servername>
For client side:  ./client localhost <clientname> <password>
where ‘hershpatel’ is the password